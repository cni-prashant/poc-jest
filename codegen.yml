overwrite: true
generates:
  sdk.ts:
    schema: 'https://app.stg.castingnetworks.com/api-gw/graphql'
    documents: './features/**/*.graphql'
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      enumsAsConst: true